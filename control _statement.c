#include<stdio.h>
int main()
{
    double amount;
    printf("enter the amount");
    scanf("%lf",&amount);

    if(discount% = (discount/amount)*100)
    {
        printf("final bill with discount");
    }

    else
    {
        printf("no discounted bill");
    }
    return 0;
}